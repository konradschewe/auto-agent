{
  "name": "auto-skill",
  "version": "0.1.0",
  "description": "Self-learning skill system — observes sessions and automatically generates reusable skills",
  "author": {
    "name": "konradschewe"
  },
  "userConfig": {
    "scope": {
      "type": "string",
      "title": "Skill scope",
      "description": "Where to write generated skills: 'both' (default), 'project' (project-local only), or 'user' (global only)",
      "default": "both"
    }
  }
}
