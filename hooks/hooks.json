{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "npx --prefix ${CLAUDE_PLUGIN_ROOT} tsx ${CLAUDE_PLUGIN_ROOT}/src/hook.ts",
            "timeout": 60,
            "statusMessage": "auto-agent: analyzing session..."
          }
        ]
      }
    ]
  }
}
